cmake_minimum_required(VERSION 3.22.1)
project(pdf-parser)

set(CMAKE_C_STANDARD 11)

set(SOURCE_FILES main.c)

add_executable(pdfparser ${SOURCE_FILES})
  
find_package(ZLIB)
target_link_libraries(pdfparser ZLIB::ZLIB)